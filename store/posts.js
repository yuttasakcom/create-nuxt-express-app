const state = {
  loadedPosts: []
};

const mutations = {};

const actions = {};

const getters = {
  loadedPosts({ loadedPosts }) {
    return loadedPosts;
  }
};

export default {
  state,
  mutations,
  actions,
  getters
};
